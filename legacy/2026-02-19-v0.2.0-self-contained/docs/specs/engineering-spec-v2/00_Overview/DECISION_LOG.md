# 决策记录（Decision Log, v2）

> 说明：记录本轮“不可隐含”的关键决策，避免后续实现与文档漂移。

| ID | 决策 | 备选项 | 选择 | 理由 | 代价/权衡 | 日期 |
|---|------|--------|------|------|-----------|------|
| D-001 | 需求真相源与主线 | 以既有 bridge-only PRD/Spec 为主；或以 CODEX_PROMPT 为主 | 以 `instructcontext/1-true-CODEX_PROMPT.md` 为主线 | 它定义了 capability-oriented v0.2.0 的完整包结构与验收口径 | 需要重写主入口与协议体系，短期破坏性较大 | 2026-02-18 |
| D-002 | 破坏式升级的处置方式 | 直接删除旧资产；或归档追溯 | 旧 bridge-only 代码/测试/规格归档到 `legacy/` | 保留追溯证据，同时消除认知噪音与错误入口 | 需要维护迁移说明与索引映射 | 2026-02-18 |
| D-003 | 框架边界：去人机交互概念 | 在框架内定义 approvals/review 等；或全部交由业务层 | 新主线不出现任何 approve/review/human interaction 概念 | 框架职责是“声明/执行/组合能力”，业务层决定如何消费结果 | 参考应用或宿主需要自行承接交互流程 | 2026-02-18 |
| D-004 | v0.2.0 交付范围 | 直接做“完整上游桥接系统”；或先交付 runtime 核心库 | 先交付 capability runtime 核心库（不在本轮实现完整上游桥接） | 降低交付风险，先把协议/执行内核做成可审计、可回归基座 | 上游桥接能力需要后续迭代补齐与验证 | 2026-02-19 |
| D-005 | `SkillSpec.source_type=\"uri\"` 生产策略 | 默认允许 uri；或默认禁用并按 allowlist 授权 | 默认禁用 uri（allowlist 为空即全禁），仅 allowlist 命中才允许读取 | safe-by-default，避免未授权远程/本地 URI 被误加载 | 需要显式维护 allowlist，配置门槛略升 | 2026-02-19 |
