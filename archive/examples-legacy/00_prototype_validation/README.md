<div align="center">

[English](README.md) | [中文](README.zh-CN.md)

</div>

# Framework Validation Prototype（已迁移）

本目录的旧实现用于验证 v0.4.0 的 `SkillSpec/SkillAdapter`（含 `inject_to` / `dispatch_rules`）。

## 状态

- **方案2 已移除 Skill 原语**：本仓库不再提供 `SkillSpec` / `SkillAdapter`。
- 因此本目录的旧版交互式原型已不再可运行。

## 替代入口

请使用参考应用：

- `archive/projects/agently-skills-web-prototype/`

它更贴近主线定位（Host 侧参考实现 + `agent_sdk` 技能引擎真相源 + NodeReport/WAL 证据链）。

