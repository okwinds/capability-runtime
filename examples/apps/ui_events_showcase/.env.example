# Real mode（后续切片实现）：需显式开启门禁并提供 OpenAI-compatible 配置。
#
# 门禁（默认关闭）：
#   CAPRT_TEST_E2E_BRIDGE=1
#
# 真实模型配置（示例；值仅占位，勿提交密钥）：
#   OPENAI_API_KEY=***
#   OPENAI_BASE_URL=https://api.openai.com/v1
#   MODEL_NAME=gpt-4o-mini

